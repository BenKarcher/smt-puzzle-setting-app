/**
 * OPTIONAL FILE: Usually NOT needed for global rules - delete this file
 *
 * Global rules typically don't need visual rendering because they affect the entire grid.
 * Only include this file if your global rule needs to draw something on the canvas.
 *
 * Most global rules (like "all rows unique" or "no adjacent shading") don't need rendering.
 *
 * @param {Array} groups - Empty array for global rules (no groups)
 * @param {CanvasRenderingContext2D} ctx - Canvas 2D context for drawing
 * @param {Object} helpers - Helper functions and constants
 */
export default function render(groups, ctx, helpers) {
  const { cellSize } = helpers;

  // Usually not needed for global rules
  // Only implement if your global rule needs visual feedback
}
